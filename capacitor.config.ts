import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plptracker.app',
  appName: 'PLP tracker',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
